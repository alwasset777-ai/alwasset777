/* ============================================================
   إعدادات Supabase — عدّل هذا الملف فقط (لا حاجة لإعادة البناء)
   1) Project URL  : Supabase → Project Settings → API → Project URL
   2) anon public  : نفس الصفحة → Project API keys → anon public
   ⚠️ لا تضع مفتاح service_role هنا أبدًا (سرّي).
   ============================================================ */
window.__ALWASSET_SUPABASE__ = {
  url: "",      // مثال: "https://abcdefgh.supabase.co"
  anonKey: "",  // مثال: "eyJhbGciOiJIUzI1NiIsInR5cCI6..."
};
